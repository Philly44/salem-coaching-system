# Force building native modules from source in WSL
build-from-source=true
# Use proper architecture
target_arch=x64
target_platform=linux
# Increase timeout for slow builds
fetch-timeout=300000
# Use node-gyp for native builds
node-gyp=/usr/bin/node-gyp